#include "libasm.h"

int 	main()
{

	// char src[400];
	// char dest[100];
	// char src2[400];
	// char dest2[100];
  
	// memset(dest, '\0', sizeof(dest));
	// strcpy(src, "hello world This is the pointer to the destination array where the content is to be copied");
	// strcpy(dest, src);
	// printf("Final copied string : |%s|\n", dest);
	// printf("--------\n");
	// memset(dest2, '\0', sizeof(dest2));
	// ft_strcpy(src2, "hello world This is the pointer to the destination array where the content is to be copied");
	// ft_strcpy(dest2, src2);
	// printf("Final copied string2 : |%s|\n", dest2);
	
	return (0);
}
